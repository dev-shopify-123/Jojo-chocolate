<div class="page-width">
  <div class="grid">
    <div class="grid__item medium-up--five-sixths medium-up--push-one-twelfth">
      <div class="section-header text-center">
        {% unless page.handle == "jojo" %}
          <h1>{{ page.title }}</h1>
        {% endunless %}
       
      </div>

      <div class="rte">
        {{ page.content }}
      </div>
    </div>
  </div>
</div>
